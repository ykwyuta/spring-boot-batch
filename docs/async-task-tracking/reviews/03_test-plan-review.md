日時: 2026-06-27 13:51:53
対象ドキュメント: docs/async-task-tracking/03_test-plan.md（コミット e079cda）
参照（承認済み）: docs/async-task-tracking/02_design.md（コミット 4cceba5、設計レビュー結論: 承認）
レビュー観点: 設計書「9. 異常系の分岐一覧」の全分岐がC1観点で網羅されているか（対応表の正確性）、
  各テストケースの期待結果が設計書の記載と整合しているか、設計レビュー指摘事項の反映状況、
  テストレイヤー分割の妥当性・実装可能な粒度、RT-04（境界値ケース）の扱いの妥当性
指摘事項:
  - 分岐総数の誤記（要修正）: 本書の冒頭（3〜4行目）および末尾（268行目）で「設計書『9』の分岐表
    に挙げられた…全分岐（A0a〜A15、B1〜B6、C0a〜C8、D1〜D3、計34項目）」と記載しているが、
    実際にA0a〜A15（17項目）、B1〜B6（6項目）、C0a〜C8（10項目）、D1〜D3（3項目）を数えると
    合計36項目になる。設計書側（02_design.md 872〜873行目）の正常系・異常系の分類列挙（異常系
    15項目＋正常系/ニュートラル21項目）も合計36項目であり、設計書の実際の分岐数と本書の記載数
    （34）が一致していない。幸い末尾の分岐対応表（229〜267行目）自体には36項目すべてが正しく
    列挙されており、C1網羅の実質には影響していないが、「全分岐に対応付けた」という結論を支える
    数字が誤っている状態であり、レビュー観点（対応表が正確か）に照らして看過できない。
  - A1（JSONパース不能、`HttpMessageNotReadableException`）に対応する専用テストケースが存在しない
    （要修正）: 本書25〜31行目の説明、および末尾の分岐対応表233行目で、A1は「既存
    `HttpMessageNotReadableException`捕捉は既存ハンドラ再利用のため、AC-01〜AC-07のバリデーション
    系ケースで`@Valid`経路を確認すれば十分」とされている。しかしAC-01〜AC-07（35〜41行目）は
    いずれもBean Validationエラー（`MethodArgumentNotValidException`）を発生させるケースであり、
    A1が指す「リクエストボディがJSONとして不正な形式（デシリアライズ自体が失敗）」
    （`HttpMessageNotReadableException`）とはスローされる例外クラス・コード経路が異なる
    （`@Valid`評価に到達する前にデシリアライズ段階で失敗するケースであり、AC-01〜AC-07では
     一度も再現されない）。本書は冒頭5〜6行目で「書式は既存の同期API機能のテスト計画
    （`docs/service-bash-invocation/03_test-plan.md`）に準拠する」と宣言しているが、参照先の
    既存テスト計画にはCT-01（「リクエストボディがJSONとして不正な形式の場合」を専用に再現する
    ケース）およびEH-05（対応するハンドラメソッドの単体検証）が存在する。本書はこれに相当する
    専用ケースを欠いており、EH-A04（166行目、既存ハンドラとの優先順位確認）の対象にも
    `HttpMessageNotReadableException`は含まれていない。これはC1（分岐網羅）上の実質的な漏れであり、
    「専用ケースを設けるかは実装フェーズでAC-01に統合してよい」という記載のみでは、実装時に
    A1の分岐（JSONパース不能時に既存ハンドラへ正しくディスパッチされること）が一度も実行
    確認されないリスクが残る。
対応: 「要修正」と判断する。承認に進めるためには以下の修正が必要。
  1. 本書3〜4行目・268行目の「計34項目」を「計36項目」に修正する（分岐番号の列挙自体は正しいため、
     数値表記のみの修正で済む）。
  2. セクション1（`AsyncTaskExecutionController`のテストケース）に、A1専用のケース（例: AC-00）を
     追加する。入力条件は既存CT-01に準拠し「`POST /internal/tasks/execute-async`に
     `Content-Type: application/json`で本文`{invalid json`を送信」、期待結果は
     「HTTPステータス`400 Bad Request`。`errorCode`が既存ハンドラの組み立てる値
     （既存`handleHttpMessageNotReadableException`の実装に準拠、`docs/service-bash-invocation/
     02_design.md`の表記と一致する値）になること。`AsyncTaskExecutionService`は呼び出されない」
     とする。末尾の分岐対応表233行目（A1の行）もこの新規ケースIDに更新する。
  3、上記2点を反映後、再レビューにより承認可否を確認する。
  なお、以下はいずれも問題なく反映されていることを確認済みであり、修正不要である。
  - 設計レビュー指摘(a)（同期API・非同期APIの挙動差異の明示的検証）: AE-08（95〜96行目）・
    IG-02（199行目）として明確に計画されており、期待結果も設計書8.3節の記載
    （非同期APIでは422が一度も返らない）と一致している。
  - 設計レビュー指摘(b)（A7/A8の区別）: AS-01/AS-02（65〜76行目）として個別ケース化され、
    73〜76行目の注記で「処理結果は同一だが入力経路が異なることを確認する」という設計レビュー
    指摘事項2への対応方針が明記されている。
  - 設計レビュー指摘(c)（TASK_NOT_FOUNDの複数文脈での再利用）: AE-03（91行目）が
    `AsyncTaskRecord.errorCode`という内部状態としての扱いとレスポンスへの非露出を明記し、
    GC-02/GS-02（非同期APIの404、C2/C4）・既存`TaskExecutionErrorCode`（既存同期API向け）との
    混同が生じないよう整理されている。
  - テストレイヤー分割（セクション1〜8）は設計書「2. パッケージ構成・クラス構成」の
    クラス一覧（`AsyncTaskExecutionController`/`AsyncTaskExecutionService`/
    `AsyncTaskExecutionStateStore`/`GlobalExceptionHandler`/`AsyncTaskRetentionScheduler`）と
    1対1で対応しており、実装工程でテストクラスへそのまま落とし込める粒度になっている。
  - RT-04（177〜186行目）はD1〜D3に直接対応しない追加の境界値ケースであるが、その位置付け
    （境界値の橋渡し）と、実装時に`removeIfOlderThan`の比較演算子の実装意図に応じて期待値を
    確認・修正する旨が181行目・183〜186行目の注記に明記されており、扱いは妥当である。
結論: 要修正

---

日時: 2026-06-27 13:56:48
対象ドキュメント: docs/async-task-tracking/03_test-plan.md（コミット 965b77c）
参照: 前回レビュー（本ファイル上記、コミット ff28777、結論: 要修正）、
  docs/async-task-tracking/02_design.md（コミット 4cceba5、設計レビュー結論: 承認）、
  docs/service-bash-invocation/03_test-plan.md（CT-01/EH-05）
レビュー観点: 前回指摘2点（分岐総数の誤記、A1専用ケースの欠落）が正しく反映されているか、
  追加したAC-00の入力条件・期待結果が既存同期API（CT-01/EH-05）と整合しているか、
  分岐対応表のA1行の更新内容、セクション9のケース数まとめの集計値の正確性、
  前回承認済みの他の指摘事項対応（同期/非同期API挙動差異、A7/A8区別、TASK_NOT_FOUND整理）が
  今回の修正によって損なわれていないか
指摘事項: なし。前回指摘の2点はいずれも意図通り反映されていることを確認した。
  - 「計34項目」の誤記修正: 冒頭4行目「…D1〜D3、計36項目）をもとに…」、末尾277行目
    「…D1〜D3、計36項目）に対し…」のいずれも「計36項目」に修正されており、文書中に
    「計34項目」の表記は残っていない（grep確認済み）。
  - AC-00の追加: セクション1冒頭の説明文（31〜36行目）でA1の扱い（既存ハンドラ再利用、
    AC-01〜AC-07とは例外クラス・コード経路が異なるため専用ケースが必要）が明確に述べられ、
    表内にAC-00（40行目）が追加されている。入力条件「`POST /internal/tasks/execute-async`に
    `Content-Type: application/json`で本文`{invalid json`を送信」は既存CT-01
    （`docs/service-bash-invocation/03_test-plan.md` 34行目、`POST /internal/tasks/execute`に
    同条件で送信）とエンドポイントのみ異なり条件は同一であり、整合している。期待結果
    「HTTPステータス`400 Bad Request`。`errorCode="BAD_REQUEST"`」も、CT-01
    （`errorCode="BAD_REQUEST"`）・EH-05（同上、`docs/service-bash-invocation/03_test-plan.md`
    74行目）・既存設計書`docs/service-bash-invocation/02_design.md`の`errorCode`対応表
    （302行目、`BAD_REQUEST`/400/JSONパース不能）と完全に一致しており、値の整合性に問題はない。
    さらに「`AsyncTaskExecutionService`は呼び出されない」という記述は、既存ハンドラが
    `@Valid`評価やService呼び出し以前のデシリアライズ段階で例外を捕捉することの確認として
    妥当である。セクション6（166〜169行目）にも「A1の分岐自体はセクション1のAC-00で
    再現・確認する」旨が明記され、新規ハンドラケースとして重複立項しない方針が一貫している。
  - 分岐対応表のA1行: 242行目が「A1 | AC-00 |」に更新されており、前回指摘の暫定的な文章記述
    （「既存ハンドラ再利用のため専用ケースを設けるかは実装フェーズで判断」）は解消されている。
  - セクション9のケース数まとめの集計値の検証: 各セクションの実際のケースID数を数えたところ、
    セクション1（AC-00〜AC-12）=13件、セクション2（AS-01〜AS-07）=7件、セクション3
    （AE-01〜AE-08）=8件、セクション4（SS-01〜SS-04）=4件、セクション5.1（GC-01〜GC-02）=2件、
    セクション5.2（GS-01〜GS-06）=6件、セクション6（EH-A01〜EH-A04）=4件、セクション7
    （RT-01〜RT-04）=4件、セクション8（IG-01〜IG-04）=4件であり、表内の記載値
    （13/7/8/4/2/6/4/4/4）と完全に一致する。合計は13+7+8+4+2+6+4+4+4=52であり、表の「合計52」
    （IG-04を除いた必須ケースのみでは52-1=51）の記載とも一致し、計算ミスはない。
  - 分岐対応表（235〜276行目）を再確認したところ、A0a〜A15（17項目）、B1〜B6（6項目）、
    C0a〜C8（10項目）、D1〜D3（3項目）の合計36行が正しく列挙されており、「計36項目」という
    本文の記載と実際の表の行数が一致している。
  - 前回承認済みの他の指摘事項対応（同期/非同期API挙動差異のAE-08/IG-02、A7/A8区別のAS-01/AS-02、
    TASK_NOT_FOUND多義性整理のAE-03の記述）は、今回の差分（git diff ff28777 965b77c）に含まれて
    おらず、無変更のまま維持されていることを確認した。テストレイヤー分割・RT-04の扱いについても
    変更はなく、前回確認した妥当性がそのまま保たれている。
対応: 指摘なし。前回指摘の2点（分岐総数の誤記、A1専用ケースの欠落）はいずれも反映済みであり、
  新たな不整合も確認されなかったため、対応不要。
結論: 承認
